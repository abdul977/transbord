<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="@android:color/transparent">

    <!-- Shadow background for better visibility -->
    <View
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_fab_shadow" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_overlay_record"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/tap_to_record"
        android:src="@drawable/ic_mic"
        android:alpha="1.0"
        app:backgroundTint="@color/primary"
        app:fabCustomSize="64dp"
        app:maxImageSize="32dp"
        app:elevation="16dp"
        app:borderWidth="0dp"
        app:rippleColor="@color/white"
        app:tint="@color/white"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Transbord.FloatingButton" />

</FrameLayout>
